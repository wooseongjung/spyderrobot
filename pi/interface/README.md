# pi/interface/

Operator-facing software on the Pi.

Planned:
- `joystick.py` — read PS2 joystick inputs
- `servo/` — PCA9685 driver wrapper, gait / pose generators
- `e_stop.py` — emergency stop binding

> Populated incrementally as Phases 1, 2, and 5 progress.
