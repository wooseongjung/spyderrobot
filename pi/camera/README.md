# pi/camera/

Capture scripts for the Raspberry Pi AI Camera (Sony IMX500).

> Populated in **Phase 5** when the camera is wired to the integrated system.

Planned:
- `capture.py` — single-shot and periodic capture
- `live.py` — push the latest frame to the dashboard
- `captures/` — saved frames (gitignored except for one demo frame)
